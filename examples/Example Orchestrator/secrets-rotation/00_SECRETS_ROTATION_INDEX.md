# Example Orchestrator - Secrets Rotation

*Stub. This orchestrator's secret INVENTORY (which secrets, which sinks) + a rotation SCHEDULE / cadence + pointers to the canonical secrets map, rotation runbook, and the secret manager. NEVER a secret value in the knowledge base.*

`<placeholder>` Fill the inventory + cadence (shape from [orchestrator-standard](../../../the-standard/orchestrator-standard.md) §3.6):
- Secret inventory: `<secret name>` -> `<sinks that consume it>`
- Rotation cadence: `<every N days / on event>`
- Canonical pointers: `<secrets map>` - `<rotation runbook>` - `<secret manager (holds values)>`

*Created by Alex Villarroel · part of Orchestrator OS.*
